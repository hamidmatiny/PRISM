# FSM runtime knobs only (Phase 18).
# Trip thresholds are source-of-truth in policies/rego/*.rego — do not re-add
# quarantine/QA/drift thresholds here and expect them to be evaluated.
# quarantine_rate.window_batches MUST match prism.quarantine_rate.window_min.
policies:
  quarantine_rate:
    window_batches: 5
cooldown_seconds: 8
