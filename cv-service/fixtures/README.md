# CV fixtures

Synthetic PNG samples under `images/` plus `manifest.json`.

These exist so unit/integration tests can assert **schema-valid, confidence-bounded**
service output. They are **not** a labeled accuracy benchmark and must not be
cited as evidence of real-world detection performance.
