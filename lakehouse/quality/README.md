# Quality expectations

`expectations.yaml` is the canonical data-quality manifest.

It is mirrored into `src/prism_lakehouse/data/expectations.yaml` for install/Docker
parity — keep both identical (enforced by `tests/unit/test_lakehouse.py`).
