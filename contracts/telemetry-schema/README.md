# telemetry-schema

**Status:** stub (Phase 0) — schemas land in Phase 1.

Will define (Pydantic + JSON Schema export):

- Sensor ping events (typed ranges, regex-validated IDs, required timestamp)
- Camera frame metadata (asset/device id, capture time, storage URI reference)

**Health / port:** N/A (library package, not a service).
