# cv-finding-schema

**Status:** stub (Phase 0) — schemas land in Phase 1 / 3.

Will define defect/anomaly findings:

- asset id, frame ref, defect class, confidence
- bounding box / segmentation mask ref
- reviewed flag (human-review workflow hook)

**Health / port:** N/A (library package, not a service).
