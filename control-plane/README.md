# control-plane

Django 5.x + Django Ninja control plane (assets, work orders, inspection review, RBAC). Lands in **Phase 5**.

| | |
|---|---|
| **Port (host)** | `9100` |
| **Health** | `GET /health` (Phase 5) |
| **DB** | Postgres via `DATABASE_URL`; SQLite local fallback |
| **Standalone** | TBD Phase 5 |

Phase 0: directory + README only.
